# Repaired trace-to-dataset campaign — 2026-09-09

This cycle repairs task definitions and collection evidence after the [Fable review](FABLE-REVIEW.md). The result directory retains its initial r3 name; the full campaign uses the separately pinned revision-5 dataset. Historical task versions, trial receipts, grades, and canonical generations retain their original meaning. The user authorized committing the reviewed implementation, aligned documentation, and evidence. No push, training job, paid operation, or inference configuration change was performed.

## Task and execution identity

The 48-instance benchmark covers twelve families, with six repository and six business families. Frozen family splits contain both domains. Revision 5 exposes exact output schemas without oracle values, limits root tools during logged prompt assembly and execution, configures one-shot background delegation with jobs collection, and requires each workflow child’s complete result in launch order. ASCII-escaped Unicode fixtures preserve combining sequences through the deployed tokenizer’s NFC normalization. The deterministic-v6 grader is unchanged.

The [pinned task receipt](benchmark-r5/tasks-receipt.json) identifies the dataset. The [campaign manifest](baseline-r5/manifest.json) binds the working-tree diff, status, untracked source bytes, proxy, all built files including the overlay, and templates. Exact executable bytes are retained under private `.implementation/` directories. The collection base is `8bc12e2e0b606a2a429e7496e428821dac0dae65`; collection included uncommitted artifact changes, so that revision alone is not its identity.

The twelve-family preflight passed actual forbidden-tool rejection for roots and children. Its [proxy observation](preflight-r5/preflight-traffic.json) contains zero events. A [negative control](preflight-negative/negative-control.json) rejects an unavailable allowed tool. The r5 [acceptance pilot](pilot-r5/pilot-report.json) passed seven of twelve trials, including workflow and Unicode. Its [provider fidelity report](pilot-r5/provider-fidelity.json) matches 41 settled agent requests; twelve auxiliary title requests remain explicitly classified. All 34 pilot source files remained unchanged.

## Inference and renderer evidence

Read-only host inspection confirmed the NVFP4 checkpoint and tokenizer revision, active FP8-origin chat template, and engine arguments in [route evidence](deployed-route.json), [engine command](engine-command.json), and [tokenizer hashes](deployed-tokenizer-hashes.log). The active template differs from the NVFP4 checkpoint’s packaged template. Local HF downloads use the pinned checkpoint tokenizer and the observed active template. The engine accepts eight concurrent sequences; this campaign’s proxy caps actual forwarded requests at four. The host guide’s cap-four statement and the observed engine argument differ. Historical comparisons therefore do not isolate prompt changes causally.

The [renderer acceptance](renderer-acceptance.json) contains five explicitly synthetic controls: a tool trajectory, a final answer, earlier reasoning with omitted target reasoning, a recovery marker, and a 44,514-token example. Each passes request-token and full-example token parity against the self-hosted `/engine/tokenize` endpoint, with exact local final-answer loss masks. The synthetic source strips answer whitespace explicitly and recomputes content hashes; it is not a transformed publication candidate.

The inspected engine reads earlier reasoning from `reasoning`, while the local template reads `reasoning_content`. The [installed-source evidence](engine-reasoning-fields.log) and [mismatch diagnostic](renderer-token-divergence.json) identify the helper’s transport mismatch. The local renderer helper maps these fields explicitly; no inference restart was needed.

Under the default exact policy, all twelve r5 pilot final answers, including all seven eligible final answers, are rejected because the active template trims target whitespace. The [real-candidate scan](pilot-r5/real-renderer-scan.json) retains those rejections. Tool-decision loss masks remain unsupported. Engine tokenization does not verify a trainer’s loss implementation, and sampled generation token IDs are unavailable. No collected candidate is approved for production thinking training or exact-token RL.

Fireworks managed SFT/DPO remain supported exports. Remote renderer verification is deferred: the [documented procedure](https://github.com/fw-ai/cookbook/blob/main/skills/fireworks-training/references/renderer-verification.md) includes a paid inference preflight, and [rendered training samples](https://docs.fireworks.ai/fine-tuning/fine-tuning-models) depend on a training job. Neither was authorized or executed.

## Phoenix cleanup and durability

Cleanup removed forty projects and eleven disposable datasets, including `gh-genai-traces`, unused discovery/validation projects, and seven hidden orphan experiment projects. [Primary deletion receipts](cleanup-receipt.json) and [orphan receipts](cleanup-orphan-receipt.json) record each target. Surviving experiments and protected baseline live/replay projects were retained. The r3 and r4 pinned task datasets remain evidence of distinct revisions.

The [additional receipt checks](cleanup-extra-receipt-checks.json) and [restored revision comparison](cleanup-retained-revision-comparison.json) cover the older datasets omitted from the first receipt audit. All 588 inspected historical example revisions match the pre-cleanup dump. The older canonical dataset’s deleted source-project spans exist only in that private local dump. Its [restore receipt](restore-before-receipt.json) verifies the dump and retained source archive. These backups are on this disk; no off-host durability is established.

## Completed campaign and publication

All 144 trials have receipts: 111 pass, 30 fail task requirements, and three time out at the runner’s 300-second limit. The timeouts are workflow-3 repetition 3 and long-context-3 repetitions 2 and 3. They remain failures with original evidence; no selective retry replaces them. The native [experiment](http://127.0.0.1:6006/datasets/RGF0YXNldDoxOA==/experiments/RXhwZXJpbWVudDoxMA==) contains all 144 runs. The [statistical report](baseline-r5/experiment-report.json) records four-instance family estimates and Wilson intervals. Failure-owner labels are heuristic triage, including historical task-specification labels; they do not establish causality.

The audit reconstructs 201 sessions and grades 144 roots. All 402 source files retain their original hashes. Replay imports all 201 sessions without telemetry errors. The supervisor stops its owned process group after the durable audit summary; the resulting -15 exit is recorded, not presented as a clean launcher exit. The unchanged grader admits 300 targets: 111 final answers and 189 independently graded tool decisions, with zero split conflicts. The [curated dataset](http://127.0.0.1:6006/datasets/RGF0YXNldDoxOQ==) has its own [immutable receipt](baseline-r5/curated-receipt.json).

Installed-adapter fidelity matches all 524 settled agent requests byte-for-byte. Every one carries medium reasoning with thinking enabled; 144 auxiliary title requests explicitly disable thinking. The [proxy summary](baseline-r5/proxy-summary.json) records a maximum of four forwarded requests, 668 HTTP-200 responses, and three admissions without a response record. Those three bodies cannot be compared and are retained as explicit incomplete evidence. No request body remains unclassified among the recorded responses.

## Accepted renderer option

Explicit `--target-whitespace template-trim` now passes request and full-example engine parity for 123 unchanged real rows: twelve pilot answers and all 111 eligible baseline final answers. Each removes exactly leading `\n\n` and no trailing content. The [acceptance file](renderer-real-acceptance.json) binds candidate, renderer, template, tokenizer, transformation, token, and mask identities. A one-character negative control produces different engine tokens. The [CLI smoke](renderer-cli.log) reports full-example parity through the documented command path. The [Fable dispositions](FABLE-REVIEW.md#renderer-design-review) record the accepted design revisions.

Canonical candidates and dataset versions remain unchanged. Only rendered supervision uses the explicit destination transformation. Control-token strings, extra text blocks, incompatible template changes, noncontiguous masks, and decode mismatches fail closed. Final reasoning and the end-of-message token remain unmasked by the format objective. This resolves the observed whitespace serialization blocker; production thinking suitability, trainer loss behavior, tool-decision masks, and exact-token RL remain unapproved.

## Validation and final backup

The [final Phoenix verification](final-phoenix-verification.json) confirms the new task and curated receipts plus the retained baseline-r2 curated receipt. Independent database counts confirm [144 native runs and 576 evaluations](native-experiment-counts.json). Both v3 evidence projects have non-expiring retention. The final backup’s [restore receipt](restore-final-receipt.json) verifies seven source inventories containing 997 files and the dataset/experiment table hashes in an isolated database; the temporary database was then dropped. The private `.backup-before` and `.backup-final` directories are retained. Redundant extracted restore copies were removed only after their receipts were copied and compared. Neither backup has an established off-host copy.

Validation completed: artifact build and typecheck; 66 artifact tests passed with four optional skips; the Python suite ran 24 tests with eight optional skips; all twelve focused tokenizer/renderer tests passed with the pinned local tokenizer; and both pilot and full installed-adapter fidelity checks passed. The final twelve-family preflight again observed zero proxy events. The documented renderer CLI reports full-example parity. Repository lint and `git diff --check` passed. A credential-value scan found no local credential values in changed or untracked non-ignored artifact files.

Documentation verification is partial: `pnpm run test:docs` passed 14/15 checks and `pnpm run doc-sync` passed 32/33. Both fail the unchanged repository Markdown globber under Node 24.12.0 with `ENOTDIR` on `snapshots/acp/image-compaction/system-prompt.expected.md/system-prompt.expected.md`. The [direct changed-file Markdown check](changed-markdown-check.log) passes using the repository Markdown parser without that globber. Bilingual pairing, links, and the remaining documentation checks passed. No upstream product file was changed to bypass the host failure.

## Next session

The [handoff’s pending-work table](../../plugins/gh-genai-traces/HANDOFF.md#pending-work) owns the next actions and authorization limits. The completed campaign, cleanup, and renderer acceptance do not require repetition to begin that work. Preserve the historical exact-policy rejection scan alongside the accepted template-trim evidence.

The final documentation alignment re-recorded all three edited bilingual pairs. Fresh [quick checks](checkpoint-docs-quick.log) again passed 14/15 and [full documentation checks](checkpoint-doc-sync.log) passed 32/33, with only the same Markdown-glob failure. [Repository lint](checkpoint-lint.log) passed, and the [direct paragraph check](checkpoint-markdown-check.log) passed for all eighteen changed Markdown files. Public command-log whitespace is cleaned for the repository whitespace check; [formatting hashes](command-log-formatting.json) identify the original private log bytes.
