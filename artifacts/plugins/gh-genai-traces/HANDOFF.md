# Trace-to-dataset checkpoint handoff

## Resume here

This checkpoint continues from `588162d2e8` on `fork/qwen38-deployment`. All changes are under `artifacts/`. Both Fable 5.1 review rounds and their fixes are recorded in the [review report](../../results/trace-pipeline-v3/FABLE-REVIEW.md). After the second review, the user authorized fixing the remaining findings and committing this checkpoint. Push remains unauthorized. No paid operation or training job is authorized. Read the [continuation report](../../results/trace-pipeline-v3/CONTINUATION.md), preserved [checkpoint](../../results/trace-pipeline-v3/REPORT.md), [experiment guide](experiments/README.md), and [Opus findings](../../OPUS_FINDINGS.md). `artifacts/KIMI_FINDINGS.md` was absent during this continuation.

## Completed continuation

The restored self-hosted route completed 144 medium trials from the frozen 48-instance, 12-family benchmark. The runner's proxy capped actual forwarded requests at four. All 206 canonical sessions were reconstructed; 412 retained source files remained unchanged. The audit imported telemetry for all 206 sessions with zero replay errors. Final regrading reused the same source evidence with replay disabled. The audit profile remains running after writing its summary, so the final local profile was stopped after verifying durable output; that interruption is not a clean process-exit check.

Task-specific grading checks read-only delegation/workflow child evidence, workflow call/run/result ordering, and independently selected tool calls. Compaction, approval exclusion, provider corruption, candidate corruption, and early auxiliary-child presentation have focused regressions. Candidate readers check hashes and explicit targets. Publication validates selected requests against source-bound provider evidence, reconciles native example revisions, and checks frozen groups across campaigns. The [decision note](DESIGN.md) explains the lossless JSONB field encoding and the single-writer publication assumption.

Installed-adapter replay matched all 593 settled baseline requests byte-for-byte. The retained unmatched records comprise 144 auxiliary title requests and eight client-disconnected requests without settlement; they are not silently dropped. Two additional admission-only proxy IDs have no response body and cannot be compared. Historical selected recordings also matched 32 requests. The helper captures before dispatch and performs no inference.

Phoenix owns the recorded 144-run experiment and the curated 126-target dataset: 57 final answers and 69 tool decisions, split 96 train / 18 validation / 12 test. Their current receipts are `baseline-r2/experiment-receipt.json` and `baseline-r2/curated-receipt.json` under the result directory. The task receipt remains `benchmark-r2/tasks-receipt.json`. Earlier datasets and versions remain intact. Tool-decision targets remain neutral candidates; both destination renderers reject their unsupported loss masks.

## Interpretation and renderer limits

The baseline has 57 passing trials. Hidden allowed-tool restrictions, underspecified JSON output fields, and the configured continuable delegation protocol confound a general model-quality interpretation. Per-instance repeatability and Wilson intervals are in `baseline-r2/experiment-report.json`; failure-owner categories are triage labels, not causal proof. A repaired benchmark needs a new pinned task revision and a distinct experiment.

Read-only inspection identified the deployed NVFP4 checkpoint and the active FP8-origin template without changing inference configuration or aliases. One fixture passed `/engine/tokenize` request parity; five additional masking cases passed local tokenization only. Neither result approves the production training renderer. Fireworks `accounts/fireworks/models/qwen3p8-27b` is supervised-LoRA tunable according to read-only model inspection. Its documented renderer verification includes paid inference, so remote preview remains unexecuted. No candidate is training-ready, and exact-token RL remains unsupported.

## Evidence and remaining acceptance

The continuation report owns commands, check results, native version identities, and restore evidence. Raw payloads, canonical stores, tokenizers, virtualenvs, and backups remain private ignored files. The existing Phoenix browser GIF demonstrates the earlier benchmark version only; it does not depict the newly published curated dataset or experiment. Git alone is not a backup, and the local restore check does not establish off-host durability.

Review the [Fable dispositions](../../results/trace-pipeline-v3/FABLE-REVIEW.md) and the separately recorded deterministic-v5 re-audit. The stored baseline and published versions retain their original deterministic-v4 meaning. The final source grader is deterministic-v6; focused regressions cover its changed diagnostics and unknown-count behavior. The v5 re-audit is historical evidence, reproduced by the tracked offline verification script; it was not rerun with v6. All 126 historical candidates pass the strengthened publication gate. Continue with the documented task-revision plan and remaining renderer acceptance; do not repeat the completed baseline without a new task revision or another concrete reason. Keep renderer approval, controlled benchmark repairs, and any future training decisions separate from the verified reconstruction and publication results. Preserve historical generations, both exporters, Phoenix state, and inference configuration.
