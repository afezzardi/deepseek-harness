/** Host discovery entry for the browser-only effort control. */
export const name = 'local-effort-slider'

/** Let the client-module loader discover the adjacent package manifest. */
export function apply(): void {}
